//#include "world.hh"
#include "car.hh"
#include "submarine.hh"

using namespace std;

int main() {
//	world sim;
	//car c(vec_3d(10,0,0), 0);
	//c.speed(3);
    //c.turn(5); // turn by 5 degrees + steering error
	//c.speed(2.5);

	//submarine s(vec_3d(20,30,-3), 90, 0);
	//s.speed(5);
	//s.turn(-20); // turn 20 degrees to left

	//sim.advance(0.1); // advance by 0.1 seconds, all robots move
	
	cout << "test robot" << endl
}
